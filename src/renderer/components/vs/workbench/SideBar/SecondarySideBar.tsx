import { FC } from 'react';
import SideBarWrapper from './SideBarWrapper';

const SecondarySideBar: FC = () => <SideBarWrapper position="right">SideBar</SideBarWrapper>;

export default SecondarySideBar;
